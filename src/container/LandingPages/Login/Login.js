import React from "react";
import styles from "./Login.module.scss";

const Login = () => {
  return (
    <div>
      <h4 className={styles.title}>Hello Login</h4>
    </div>
  );
};

export default Login;
